node_modules/
dist/
*.log
.DS_Store

# Rite loop run artifacts (reports/packets are runtime output)
.rite/projects/*/loops/

# Eval run outputs (regenerated by `node dist/eval/far-ablation.js`;
# the harness in src/eval/ is tracked, the per-run JSON is not)
/eval/

# Generated tool adapters for this repo's own dogfooding install
# (.rite/ is kept — it holds the rite-loop project's planning + evidence).
/.claude/
/.agents/
/.codex/
